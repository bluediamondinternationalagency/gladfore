[build]
  command = "npm run build"
  publish = "dist"

[functions]
  node_bundler = "esbuild"
  included_files = ["server/**"]

  [functions.format]
    external_node_modules = []

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
